/**
 * Union Type : 변수안에 A 또는 B가 들어올수 있게 타입을 지정해주는 OR연산자 같은 타입
 * let 이름 string | number = 'kim';
 * 
 * 배열형
        let 어레이: (string | number)[] = [1, '2', 3];

 * 객체형
        let 오브젝트: { data: (number | string) } = { data: '123' };

    
 */
